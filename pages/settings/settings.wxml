<cu-custom bgColor="bg-brand" isBack="{{true}}">
  <view slot="backText">返回</view>
  <view slot="content">设置</view>
</cu-custom>
<view class="padding flex flex-direction">
  <button class="cu-btn round line-red lg" bind:tap="logout">退出登录</button>
</view>
